yipt:
  version: "1.0"
  content:
    - type: block
      top: 40
      left: 60
      content:
        - type: pageBreak
        - "add page in block, x: {$doc.x}, y: {$doc.y}"
    - type: pageBreak
    - "add page in root, x: {$doc.x}, y: {$doc.y}"
