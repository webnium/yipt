yipt:
  version: "1.0"
  content:
    - type: case
      conditions:
        - when: $.foo > 0
          content: foo is larger than 0.
        - when: $.bar.isTrue
          content: bar.isTrue is true.
      else: It doesn't match to any case.
